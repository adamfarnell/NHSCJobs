<div class = "container-fluid">
    <div class = "row panel">
        <% @users.each do |t| %>
        <div class ="col-xs-4 text-center">
            <h1><%= t.first_name %> <%= t.surname %></h1>
            <h3></h3>
            <%= link_to "Learn more", user_path(t) %>
        </div>
        <% end %>
    </div>
</div>